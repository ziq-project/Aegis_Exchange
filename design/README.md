# design/ — visual reference only

Imported from the Claude Design project
["Aegis Exchange UI Concept"](https://claude.ai/design/p/25213922-466c-448b-8c8c-eb0decc2049f?file=Aegis+Exchange.dc.html)
(Phase-1 panels only; the full concept with Buy/Sell/Auctions/Crafting lives in
the project).

- `overview.png` — panel 1a: Aegis tab in the AH frame + status/scan strip.
- `01-scan-strip.png` — panels 1b/1c/1j: scanning state, stale-idle warning,
  tooltip mockup.
- `aegis-exchange.dc.html` — source for the renders above.

These are a **visual target only**. The real UI is built in Lua with vanilla
1.12 frame templates per `CLAUDE.md`. Never port the HTML/CSS, and never add
anything from this folder to the `.toc`.
